# API 配置示例
# 复制此文件为 .env.local 并填入实际值

# ===== API 配置 =====
# AWS API Gateway 端点（生产环境）
VITE_API_BASE_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/dev

# 本地开发端点（可选）
# VITE_API_BASE_URL=http://localhost:3001

# ===== AWS Cognito 配置 =====
# Cognito User Pool ID (从 AWS Console → Cognito → User Pools 获取)
VITE_COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx

# Cognito App Client ID (从 User Pool → App Integration → App clients 获取)
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Cognito Domain (用于托管 UI，可选)
# 格式: https://your-domain.auth.us-east-1.amazoncognito.com
VITE_COGNITO_DOMAIN=https://your-domain.auth.us-east-1.amazoncognito.com

# AWS Region
VITE_AWS_REGION=us-east-1

# ===== 回调 URL 配置（可选，默认自动检测） =====
# 本地开发时自动使用: http://localhost:5173/callback
# 生产环境自动使用: https://ethanlita.github.io/qnyproj/callback
# 
# 仅在需要自定义时设置以下变量：
# VITE_REDIRECT_URI=http://localhost:5173/callback
# VITE_LOGOUT_URI=http://localhost:5173
# VITE_SILENT_REDIRECT_URI=http://localhost:5173/silent-renew.html
